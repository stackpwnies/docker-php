version: "2"

volumes:
    data:

services:

    web:
        build: nginx
        working_dir: /application
        volumes:
            - data:/application
        ports:
            - 80
        labels:
            "traefik.enable": "true"
            "traefik.frontend.entryPoints": "http,https"

    php-fpm:
        build: php-fpm
        working_dir: /application
        volumes:
            - data:/application
